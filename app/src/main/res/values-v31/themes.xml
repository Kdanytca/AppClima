<resources>
    <style name="Theme.AppClima" parent="Theme.Material3.Light.NoActionBar">
        <!-- Fondo blanco fijo -->
        <item name="android:windowBackground">@color/white</item>

        <!-- Desactivar el modo oscuro automático -->
        <item name="android:forceDarkAllowed">false</item>

        <!-- Colores de texto por si acaso -->
        <item name="android:textColor">@color/black</item>
    </style>
</resources>


